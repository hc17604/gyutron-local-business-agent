# ACME Industrial — MOCK customer dataset

ENTIRELY FICTIONAL sample data (replication proof for the CSV commerce
connector). Import as its own source (e.g. source_key=acme-industrial-csv) —
it must never mix with GYUTRON data.
